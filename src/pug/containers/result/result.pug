mixin result(result)
 section.result
  .container.container--result
   .row.row--border.row--result
    each it in result
     .col-lg-3.col-md-6
      .result__it(class=`result__it--${it.color}`)
       .__quan #{it.title}
       .__quan-label #{it.descr}
